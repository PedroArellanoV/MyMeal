<resources>
    <!-- Base application theme. -->
    <style name="Theme.MyMeal" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/green_logo_dark</item>
        <item name="colorPrimaryVariant">@color/green_principal</item>
        <item name="colorOnPrimary">@color/green_background</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
    </style>
</resources>